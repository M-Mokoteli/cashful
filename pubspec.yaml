name: flutter_application_1
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+8

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.4

  flutter_svg: ^1.0.3
  
  firebase_auth: ^3.3.18
  cloud_firestore: ^3.1.15
  firebase_storage: ^10.2.16
  firebase_core: ^1.17.0
  firebase_messaging: ^11.4.0
  firebase_database: ^9.0.15
  location: ^4.4.0
  flutter_contacts: ^1.1.4

  modal_progress_hud_nsn: ^0.1.0-nullsafety-1 #try to remove,very old
  fluttertoast: ^8.0.9
  flutter_form_builder: ^7.2.0
  provider: ^6.0.2
  image_picker: ^0.8.5+3
  dotted_border: ^2.0.0+2
  shared_preferences: ^2.0.15
  intl: ^0.17.0
  validators: ^3.0.0
  url_launcher: ^6.1.2
  connectivity_plus: ^2.3.0
  pin_code_fields: ^7.4.0
  get_it: ^7.2.0
  file_picker: ^4.5.1
  flutter_pdfview: ^1.2.2
  permission_handler: ^9.2.0
  usage_stats: ^1.1.1 #android only
  flutter_local_notifications: ^9.5.3+1
  lottie: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.2
  mockito: ^5.2.0
  build_runner: ^2.1.11

flutter_icons:
  android: true
  ios: false
  image_path: "assets/images/logocashful.png"

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/svg/
    - assets/lottie/
